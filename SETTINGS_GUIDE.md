# دليل استخدام صفحة الإعدادات ⚙️

## الوصول للإعدادات

1. سجل الدخول للوحة التحكم: `http://your-server:3000/admin`
2. اضغط على زر **"الإعدادات"** في الأعلى
3. أو اذهب مباشرة إلى: `http://your-server:3000/admin/settings`

---

## إعداد قاعدة البيانات (Supabase)

### 1️⃣ الحصول على معلومات Supabase

من لوحة تحكم Supabase:
1. اذهب إلى **Settings** → **API**
2. انسخ:
   - **Project URL**: `https://xxxxx.supabase.co`
   - **anon public key**: تجده تحت "Project API keys"
   - **service_role key**: اضغط "reveal" لإظهاره

### 2️⃣ إدخال البيانات في الإعدادات

في صفحة الإعدادات:
1. الصق **Project URL** في حقل "رابط المشروع"
2. الصق **anon key** في حقل "Anon Key"
3. الصق **service_role key** في حقل "Service Role Key"
4. يمكنك تفعيل "إظهار المفاتيح" لرؤية ما تكتب

### 3️⃣ اختبار الاتصال

- اضغط زر **"🔍 اختبار الاتصال"**
- انتظر النتيجة:
  - ✅ "الاتصال بقاعدة البيانات ناجح!" - البيانات صحيحة
  - ❌ "فشل الاتصال" - راجع البيانات المدخلة

---

## تحديث معلومات الدخول

### تغيير اسم المستخدم وكلمة المرور

1. في قسم **"🔐 معلومات تسجيل الدخول"**:
   - أدخل **اسم المستخدم** الجديد
   - أدخل **كلمة المرور** الجديدة (استخدم كلمة مرور قوية!)
   
2. اضغط **"💾 حفظ التغييرات"**

⚠️ **تنبيه**: بعد الحفظ، ستحتاج لإعادة تسجيل الدخول بالبيانات الجديدة!

---

## حفظ التغييرات

1. بعد إدخال جميع البيانات
2. اضغط **"💾 حفظ التغييرات"**
3. ستظهر رسالة: "✅ تم حفظ الإعدادات بنجاح!"

---

## تطبيق التغييرات

⚠️ **مهم جداً**: بعد حفظ الإعدادات، يجب إعادة تشغيل التطبيق:

### في Portainer:
1. اذهب إلى **Containers**
2. اختر الـ Container: `atayb-app`
3. اضغط **Restart**
4. انتظر 30-60 ثانية حتى يعمل

### عبر Terminal:
```bash
docker restart atayb-app
```

---

## أين تُحفظ الإعدادات؟

- الإعدادات تُحفظ في: `/app/data/config.json` داخل الـ Container
- هذا الملف محفوظ في الـ Volume: `atayb-data`
- حتى لو أعدت تشغيل الـ Container، الإعدادات ستبقى

---

## ترتيب الأولوية

التطبيق يقرأ الإعدادات بهذا الترتيب:
1. **أولاً**: ملف `config.json` (من صفحة الإعدادات)
2. **ثانياً**: المتغيرات البيئية في Portainer
3. **أخيراً**: القيم الافتراضية

---

## حالات الاستخدام

### 🔄 تغيير قاعدة البيانات
إذا أردت الانتقال لمشروع Supabase آخر:
1. احصل على بيانات المشروع الجديد
2. ادخلها في صفحة الإعدادات
3. اختبر الاتصال
4. احفظ وأعد التشغيل

### 🔐 تحديث كلمة المرور بعد النشر
بدون الحاجة لإعادة النشر أو تعديل المتغيرات البيئية:
1. اذهب للإعدادات
2. غير كلمة المرور
3. احفظ
4. سجل خروج وادخل بالكلمة الجديدة

### ⚠️ استعادة الإعدادات الافتراضية
إذا نسيت كلمة المرور أو حصل خطأ:
1. توقف عند Container
2. احذف ملف `config.json` من الـ Volume
3. شغل الـ Container مرة أخرى
4. سيستخدم المتغيرات البيئية

---

## نصائح الأمان 🔒

- ✅ استخدم كلمة مرور قوية (حروف + أرقام + رموز)
- ✅ لا تشارك مفتاح service_role مع أحد
- ✅ احفظ نسخة من الإعدادات في مكان آمن
- ✅ غير كلمة المرور بشكل دوري

---

## استكشاف الأخطاء

### المشكلة: "فشل الاتصال بقاعدة البيانات"
**الحلول:**
- تحقق من صحة Project URL (يجب أن ينتهي بـ `.supabase.co`)
- تأكد من نسخ المفاتيح كاملة (بدون مسافات زائدة)
- تحقق من تنفيذ `SUPABASE_MIGRATION.sql` في Supabase

### المشكلة: "لا يمكن الدخول بعد تغيير كلمة المرور"
**الحلول:**
- تأكد من إعادة تشغيل الـ Container بعد الحفظ
- جرب مسح الـ cookies من المتصفح
- تحقق من اللوجات: `docker logs atayb-app`

### المشكلة: "الإعدادات لا تُطبق"
**الحلول:**
- تأكد من إعادة تشغيل الـ Container
- تحقق من وجود الـ Volume: `docker volume ls | grep atayb-data`
- تحقق من الـ permissions على مجلد data

---

## ملخص سريع ⚡

1. **الإعدادات** → أدخل بيانات Supabase
2. **اختبار الاتصال** → تأكد من نجاح الاتصال
3. **حفظ** → اضغط زر حفظ التغييرات
4. **إعادة التشغيل** → Restart Container في Portainer
5. **تأكد** → سجل دخول وتحقق من عمل كل شيء

✨ الآن يمكنك إدارة قاعدة البيانات ومعلومات الدخول بسهولة من لوحة التحكم!
