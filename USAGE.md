# 🚀 خطوات الاستخدام السريع

## بعد النشر مباشرة:

### 1. الوصول الأول (إعداد أولي)

```bash
# بعد النشر، افتح المتصفح:
http://your-server-ip:3000/admin
```

**معلومات الدخول الافتراضية:**
- Username: `admin` (أو من `.env.portainer`)
- Password: `YourSecurePassword123!` (أو من `.env.portainer`)

---

### 2. إعداد قاعدة البيانات من لوحة التحكم

#### الخطوة 1: الدخول للإعدادات
1. بعد تسجيل الدخول، اضغط زر **"⚙️ الإعدادات"** في الأعلى

#### الخطوة 2: إدخال بيانات Supabase
```
رابط المشروع (Project URL):
  https://qxhsywktcdhsmdkcdyor.supabase.co

Anon Key:
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

Service Role Key:
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

#### الخطوة 3: اختبار الاتصال
- اضغط **"🔍 اختبار الاتصال"**
- انتظر: يجب أن ترى "✅ الاتصال بقاعدة البيانات ناجح!"

#### الخطوة 4: الحفظ
- اضغط **"💾 حفظ التغييرات"**
- ستظهر رسالة نجاح

#### الخطوة 5: إعادة التشغيل
```bash
# في Portainer:
Containers → atayb-app → Restart

# أو عبر Terminal:
docker restart atayb-app
```

---

### 3. التحقق من العمل

```bash
# افتح الموقع:
http://your-server-ip:3000

# يجب أن ترى:
✅ الصفحة الرئيسية تعمل
✅ الروابط تظهر
✅ التتبع يعمل
```

---

## استخدام يومي:

### إضافة رابط جديد:
1. `/admin` → Dashboard
2. زر **"+ إضافة"**
3. أدخل البيانات
4. **حفظ**

### رفع ملفات PDF:
1. في الرابط، اختر نوع **"pdf"**
2. اضغط "رفع"
3. اختر الملف
4. **حفظ**

### مشاهدة الإحصائيات:
- في Dashboard، ستجد:
  - 👁️ عدد المشاهدات
  - 👆 عدد النقرات لكل رابط

### تحديث الهيدر:
1. في Dashboard
2. قسم **"الهيدر"**
3. رفع صورة جديدة

---

## تغيير كلمة المرور:

```bash
# الطريقة الجديدة (من لوحة التحكم):
1. /admin/settings
2. قسم "معلومات تسجيل الدخول"
3. أدخل Username + Password جديد
4. احفظ
5. سجل خروج وادخل بالبيانات الجديدة
```

---

## النسخ الاحتياطي:

### نسخ احتياطي للملفات:
```bash
docker run --rm -v atayb-uploads:/data -v $(pwd):/backup \
  alpine tar czf /backup/uploads-backup-$(date +%Y%m%d).tar.gz /data
```

### نسخ احتياطي للإعدادات:
```bash
docker exec atayb-app cat /app/data/config.json > config-backup.json
```

### نسخ احتياطي لقاعدة البيانات:
- Supabase Dashboard → Database → Backups
- أو استخدم pg_dump

---

## التحديث:

```bash
# 1. بناء صورة جديدة
docker build -t atayb-app:latest .

# 2. في Portainer:
Stack → Update → Pull latest image → Update

# أو يدوياً:
docker-compose down
docker-compose up -d
```

---

## الروابط المهمة:

```
الموقع الرئيسي:     http://server:3000
لوحة التحكم:        http://server:3000/admin
الإعدادات:          http://server:3000/admin/settings
```

---

## المشاكل الشائعة:

### 🔴 لا يمكن تسجيل الدخول
```bash
# تحقق من logs
docker logs atayb-app

# تأكد من صحة Username/Password
# راجع .env.portainer أو config.json
```

### 🔴 خطأ في الاتصال بقاعدة البيانات
```bash
# تحقق من:
1. Project URL صحيح؟
2. تنفيذ SUPABASE_MIGRATION.sql؟
3. المفاتيح صحيحة؟
```

### 🔴 الملفات لا تُرفع
```bash
# تحقق من Volume
docker volume inspect atayb-uploads

# تحقق من permissions
docker exec atayb-app ls -la /app/public/uploads
```

---

## الدعم والمساعدة:

📚 **الوثائق الكاملة:**
- `ADMIN_SETTINGS_FEATURE.md` - دليل المزايا الجديدة
- `SETTINGS_GUIDE.md` - دليل صفحة الإعدادات
- `PORTAINER_DEPLOY.md` - دليل النشر
- `QUICK_START_AR.md` - البدء السريع

---

✨ **استمتع باستخدام Atayb!**
